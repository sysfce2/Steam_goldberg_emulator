# UNLESS YOU WANNA RUN INTO VARIOUS ISSUES SOONER OR LATER, AS PROVEN REPEATEDLY ON CS RIN RU,
# YOU MUST USE A COMPLETE VERSION OF THIS FILE, WITH ALL ITS SETTINGS AND THEIR EXPLANATIONS.
# TO GENERATE THE COMPLETE GSE CONFIG FOLDER, ALWAYS USE GEN_EMU_CFG FROM GSE_TOOLS_FORK REPO!

[overlay::reshade]
# disable the ReShade startup banner ("ReShade X.X loaded" overlay notification)
# writes [OVERLAY] ShowStartupBanner=0 to the game's ReShade.ini
# only takes effect if ReShade is detected (local proxy or SK plugin)
# 1=disable the banner
# 0=leave ReShade's default behavior (default)
# default=0
disable_reshade_banner=0
# 1=enable the overlay bridge for the ReShade addon without hooking the renderer
# this allows the ReShade addon (gse_reshade_overlay_addon) to render the overlay
# even when enable_experimental_overlay=0 (the native overlay is disabled)
# the bridge only provides data (achievements, notifications, friends, etc.)
# while the ReShade addon handles all the rendering
# default=0
enable_experimental_bridge=0

[overlay::general]
# 1=enable the experimental overlay - might cause crashes or other problems, so USE AT YOUR OWN RISK
# default=0
enable_experimental_overlay=0
# 1=suppress the GameOverlayActivated_t callback when the overlay opens/closes
# many games pause gameplay when they receive this callback
# enable this if you don't want the game to pause when the overlay is open
# default=0
disable_overlay_activated_callback=0
# amount of time to wait before attempting to detect and hook the renderer (DirectX, OpenGL, Vulkan, etc...)
# default=0
hook_delay_sec=0
# timeout for the renderer detector
# default=15
renderer_detector_timeout_sec=15
# 1=disable the achievements notifications
# default=0
disable_achievement_notification=0
# 1=disable friends invitations and messages notifications
# default=0
disable_friend_notification=0
# 1=disable showing notifications for achievements progress
# default=1
disable_achievement_progress=1
# 1=disable any warning in the overlay
# default=0
disable_warning_any=0
# 1=disable the bad app ID warning in the overlay
# default=0
disable_warning_bad_appid=0
# 1=disable the local_save warning in the overlay
# default=0
disable_warning_local_save=0
# by default the overlay will attempt to upload the achievements icons to the GPU
# so that they are displayed, in rare cases this might keep failing and cause FPS drop
# 0=prevent the overlay from attempting to upload the icons periodically,
#   in that case achievements icons won't be displayed
# default=1
upload_achievements_icons_to_gpu=1
# 1=sort achievements list by global unlock percentage (requires an internet connection)
# default=0
sort_achievements_by_global_percent=0
# amount of frames to accumulate, to eventually calculate the average frametime (in milliseconds)
# lower values would result in instantaneous frametime/fps, but the FPS would be erratic
# higher values would result in a more stable frametime/fps, but will be inaccurate due to averaging over long time
# minimum allowed value = 1
# default=10
fps_averaging_window=10
# 1=always show user info in the overlay
# default=0
overlay_always_show_user_info=0
# 1=always show fps in the overlay
# default=0
overlay_always_show_fps=0
# 1=always show frametime in the overlay
# default=0
overlay_always_show_frametime=0
# 1=always show playtime in the overlay
# default=0
overlay_always_show_playtime=0
# 1=show the FPS graph in the overlay performance stats display
# default=1
overlay_show_fps_graph=1
# 1=show the frametime graph in the overlay performance stats display
# default=1
overlay_show_frametime_graph=1
# 1=show min/max/average values in the overlay performance stats display
# default=1
overlay_show_min_max_avg=1
# 1=show 1% low percentile in the overlay performance stats display
# default=1
overlay_show_percentile_1=1
# 1=show 5% low percentile in the overlay performance stats display
# default=1
overlay_show_percentile_5=1
# 1=show 0.1% low percentile in the overlay performance stats display
# default=0
overlay_show_percentile_01=0
# time window (in seconds) used for the overlay performance graphs, valid range: 1-30
# default=5
overlay_graph_timeframe_sec=5

[overlay::hotkeys]
# combination of keys used to show/hide the overlay
# available values:
#   shift
#   ctrl
#   alt
#   tab
#   fn1
#   fn2
#   ...
#   ...
#   fn12
# 
# you can use the '+' sign to combine multiple keys
# example: ctrl + shift + fn5
# 
# default=shift + tab
key_combo=shift + tab

[overlay::specialk]
# automatically start Special K's injection service when the game starts
# if SKIF is already running, it sends "Start Temp" to trigger injection
# if SKIF is NOT running, the emu will try to find and launch it automatically:
#   1. checks 'specialk_install_path' if set
#   2. checks default location: %LOCALAPPDATA%\Programs\Special K\SKIF.exe
# waits up to 10 seconds for SK to inject, the service auto-stops after injection
# 1=auto-start SK injection
# 0=don't auto-start (default)
# default=0
auto_inject_specialk=0
# path to SKIF.exe for auto-injection (used when SKIF is not already running)
# can be a full path to SKIF.exe or just the folder containing it
# if empty, the emu uses the default install location (%LOCALAPPDATA%/Programs/Special K/)
# default=(empty)
specialk_install_path=
# how long the SK injection service stays running (in seconds)
# useful for games with separate launchers/settings apps that load steam_api before the game exe
# 0=use Temp mode (auto-stop after first successful injection, best for direct game launches)
# >0=keep the service running for this many seconds then stop, giving the real game exe time to start
# example: 30 = service stays active for 30 seconds, enough for most launchers to start the game
# default=0
specialk_service_duration=0
# disable the Special K startup notification/OSD toast
# writes [SpecialK.System] Silent=true to the per-game SK profile INI
# only takes effect if Special K is detected or auto-injected
# 1=suppress SK startup notification
# 0=leave SK's default behavior (default)
# default=0
disable_specialk_notification=0

[overlay::appearance]
# load custom TrueType font from a path, it could be absolute, or relative
# relative paths will be looked up inside the local folder 'steam_settings/fonts/' first,
# if that wasn't found, it will be looked up inside the global folder 'GSE Saves/settings/fonts/'
# default=
Font_Override=Roboto-Medium.ttf
# custom font for achievement title (leave empty to use default font)
# default=
Font_Override_Achievement_Title=
# custom font for achievement description (leave empty to use default font)
# default=
Font_Override_Achievement_Description=
# global font size
# for built-in font, multiple of 16 is recommended, e.g. 16 32...
# default=16.0
Font_Size=20.0
# optional independent font sizes. <= 0 uses Font_Size
Font_Size_FPS=0.0
Font_Size_Achievement_Title=0.0
Font_Size_Achievement_Description=0.0
# 1=bold font for achievement title
# default=0
Font_Achievement_Title_Bold=0

# achievement icon size
Icon_Size=64.0

# spacing between characters
Font_Glyph_Extra_Spacing_x=1.0
Font_Glyph_Extra_Spacing_y=0.0

# background for all types of notifications
Notification_R=0.12
Notification_G=0.14
Notification_B=0.21
Notification_A=1.0

# notifications corners roundness
Notification_Rounding=10.0
# horizontal (x) and vertical (y) margins for the notifications
Notification_Margin_x=5.0
Notification_Margin_y=5.0

# duration/timing for various notification types (in seconds)
# duration of notification animation in seconds. Set to 0 to disable
Notification_Animation=0.35
# duration of achievement progress indication
Notification_Duration_Progress=6.0
# duration of achievement unlocked
Notification_Duration_Achievement=7.0
# duration of friend invitation
Notification_Duration_Invitation=8.0
# duration of chat message
Notification_Duration_Chat=4.0

# minimum time interval between achievement notifications (in seconds)
# set to 0 to show all achievements immediately without delay
# default=0
Achievement_Notification_Delay=0

# format for the achievement unlock date/time, limited to 79 characters
# if the output formatted string exceeded this limit, the builtin format will be used
# look for the format here: https://en.cppreference.com/w/cpp/chrono/c/strftime
# default=%Y/%m/%d - %H:%M:%S
Achievement_Unlock_Datetime_Format=%Y/%m/%d - %H:%M:%S

# 1=show the playtime display in the user info section of the overlay
# requires record_playtime=1 in [main::stats]
# default=0
Show_Playtime_In_User_Info=0
# main background when you press shift+tab
Background_R=0.12
Background_G=0.11
Background_B=0.11
Background_A=0.55

Element_R=0.30
Element_G=0.32
Element_B=0.40
Element_A=1.0

ElementHovered_R=0.278
ElementHovered_G=0.393
ElementHovered_B=0.602
ElementHovered_A=1.0

ElementActive_R=-1.0
ElementActive_G=-1.0
ElementActive_B=-1.0
ElementActive_A=-1.0

# ############################# #
# available options:
# top_left
# top_center
# top_right
# bot_left
# bot_center
# bot_right

# position of achievements
PosAchievement=bot_right
# position of invitations
PosInvitation=top_right
# position of chat messages
PosChatMsg=top_center
# ############################# #

# ############################# #
# FPS background color
Stats_Background_R=0.0
Stats_Background_G=0.0
Stats_Background_B=0.0
Stats_Background_A=0.6

# FPS text color
Stats_Text_R=0.8
Stats_Text_G=0.7
Stats_Text_B=0.0
Stats_Text_A=1.0

# FPS position in percentage [0.0, 1.0]
# X=0.0 : left
# X=1.0 : right
Stats_Pos_x=0.0

# Y=0.0 : up
# Y=1.0 : down
Stats_Pos_y=0.0
# ############################# #

# ############################# #
# Image gamma/colour correction before GPU upload.
# ingame_overlay forces a UNORM render-target-view on its own render pass, so sRGB-encoded
# texture bytes are written to the back buffer without any hardware gamma correction.
# The overlay now detects the swap-chain colour space automatically and applies the
# appropriate transform:
#
#   FP16 / FP32 / 16-bit UNORM (scRGB linear HDR):
#     sRGB -> linear + SDR-white-level scaling + Reinhard soft-shoulder tone map.
#
#   HDR10 (R10G10B10A2, PQ / ST.2084):
#     sRGB -> linear -> PQ encode at SDR-white nit level.
#
#   _SRGB render-target view (e.g. R8G8B8A8_UNORM_SRGB):
#     sRGB -> linear only  (hardware re-encodes sRGB on write).
#
#   8-bit SDR UNORM / DX9 / OpenGL:
#     No transform needed — pass-through.
#
# auto = apply the correct transform based on detected swap-chain format
# on   = always decode sRGB->linear (legacy; use Swapchain_Override for finer control)
# off  = never decode (original behaviour, no colour correction)
# default=auto
Image_Gamma=auto

# ############################# #
# Manual swap-chain colour-space override.
# Normally the overlay detects the back-buffer format from the first rendered frame and
# selects the right colour transform automatically (see Image_Gamma above).
# Use this setting ONLY if auto-detection gives wrong results — for example, some OpenGL games
# use GL_FRAMEBUFFER_SRGB which cannot be detected from the format alone.
#
# auto       = trust the detected format (recommended)
# linear_hdr = force scRGB / linear HDR path (sRGB->linear + SDR-white scale)
# hdr10_pq   = force HDR10 PQ path (sRGB->linear->PQ encode)
# srgb_rtv   = force _SRGB render-target-view path (sRGB->linear, hw re-encodes)
# sdr        = force plain SDR UNORM (no transform)
# default=auto
Swapchain_Override=auto
# ############################# #
